{
	"name": "rorlabs",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"workspaces": [
		"./server",
		"./client",
		"./shared"
	],
	"scripts":{
		"dev:server": "bun run --cwd server dev",
		"dev:client": "bun run --cwd client dev",
		"build:client": "bun run --cwd client build",
		"start": "bun run build:client && bun run --cwd server start"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}