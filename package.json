{
	"name": "rorlabs",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"workspaces": [
		"./server",
		"./client",
		"./shared"
	],
	"scripts": {
		"server:dev": "bun run --cwd server dev",
		"client:dev": "bun run --cwd client dev",
		"build": "bun run --cwd client build",
		"start": "bun run build && bun run --cwd server start"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.9.2"
	}
}